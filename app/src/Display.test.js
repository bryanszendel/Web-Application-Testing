import React from 'react';
import "@testing-library/react";

import Display from './Display'

describe('<Display />', () => {
  it('should return a display of strikes and balls', () => {
    expect(1 === 1) //change this
  })
})